
import AddStatistic from '@/components/StatisticComponent/AddStatistic/AddStatistic'
import React from 'react'

export default function page({params}:{params:{childSlug:any}}){
    console.log(params);
  return (
    <div>
      {/* <AddStatistic order={undefined} endDay={new Date()} startDay={new Date()} customer={null} />   */}
    </div>
  )
}
